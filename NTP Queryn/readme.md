# 🕒 NTP Time Query Tool

> A simple graphical user interface (GUI) application to query accurate time from NTP (Network Time Protocol) servers.  
> Built with **Python + Tkinter**, displays UTC time, local time, and offset compared to system clock.

---

## 🌟 Features

- Query time from any NTP server (`pool.ntp.org`, `time.google.com`, etc.)
- Display UTC time, local time, and time offset
- Non-blocking GUI using threading
- Modern, lightweight interface
- Buildable into a standalone `.exe` for Windows

---
